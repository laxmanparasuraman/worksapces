// error handling or exeption handling

// syntax  for try and catch
/* 
try{

}catch(err){

}
try : wrap up the code whic can throe the error
catch: write the code to do something when error occurs
finnaly it will always executed

*/
 try{
    let firstName="laxman";
    console.log(firstName+" "+lastname);
 }catch(e){
    console.log(e.name);
    console.log(e.message);
 }finally{
    console.log("important");
 }
 console.log("first");

 /*
  reference error
  syntax error
  typo error

   */

  // Synchoronous in single thread 
  // 
  var count =10;
  var name =
  // google scope lexigal scope scope chin scope chain
  // Hoisting : accessing something before it ios called
  // temporal dead zone
  
